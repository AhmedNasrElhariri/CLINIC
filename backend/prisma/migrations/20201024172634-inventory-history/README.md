# Migration `20201024172634-inventory-history`

This migration has been generated by etahoon at 10/24/2020, 7:26:34 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "public"."InventoryOperation" AS ENUM ('Add', 'Substract')

CREATE TABLE "public"."InventoryHistory" (
"itemId" text   NOT NULL ,
"clinicId" text   NOT NULL ,
"userId" text   NOT NULL ,
"operation" "InventoryOperation"  NOT NULL ,
"quantity" integer   NOT NULL ,
PRIMARY KEY ("itemId","clinicId")
)

ALTER TABLE "public"."InventoryHistory" ADD FOREIGN KEY ("itemId")REFERENCES "public"."Item"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."InventoryHistory" ADD FOREIGN KEY ("clinicId")REFERENCES "public"."Clinic"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."InventoryHistory" ADD FOREIGN KEY ("clinicId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201024035942-rename-cols..20201024172634-inventory-history
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource postgresql {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -53,8 +53,12 @@
   PerUnit
   Milligram
 }
+enum InventoryOperation {
+  Add
+  Substract
+}
 //////////////////////////////////////////////////////////////////////////////////////////////////////// 
@@ -344,4 +348,16 @@
   clinic        Clinic     @relation(fields: [clinicId], references: [id])
   clinicId      String
   @@id([itemId,clinicId])
 }
+
+model InventoryHistory {
+  item            Item       @relation(fields: [itemId], references: [id])
+  itemId          String
+  clinic          Clinic     @relation(fields: [clinicId], references: [id])
+  clinicId        String
+  user            User       @relation(fields: [clinicId], references: [id])
+  userId          String
+  operation       InventoryOperation
+  quantity        Int
+  @@id([itemId,clinicId])
+}
```


