# Migration `20200506011126-add-email-and-password`

This migration has been generated by etahoon at 5/6/2020, 1:11:26 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Appointment" ALTER COLUMN "glucoseLevel" SET DEFAULT -1,
ALTER COLUMN "height" SET DEFAULT -1,
ALTER COLUMN "pulse" SET DEFAULT -1,
ALTER COLUMN "temp" SET DEFAULT -1,
ALTER COLUMN "weight" SET DEFAULT -1;

ALTER TABLE "public"."User" ADD COLUMN "email" text  NOT NULL ,
ADD COLUMN "password" text  NOT NULL ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200425125700-alter-uuid-to-string..20200506011126-add-email-and-password
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs https//pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -29,15 +29,17 @@
   Secondary
 }
 model User {
-  id                 String @default(uuid()) @id
+  id                 String  @id @default(uuid())
   name               String
+  email              String
+  password           String
   specialty          Specialty
 }
 model Patient {
-  id                 String @default(uuid()) @id
+  id                 String  @id @default(uuid())
   name               String
   phoneNo            String @unique
   type               PatientMembershipType
   appointments       Appointment[]
@@ -45,9 +47,9 @@
   sex                Sex
 }
 model Appointment {
-  id                 String @default(uuid()) @id
+  id                 String  @id @default(uuid())
   date               DateTime @unique @default(now())
   type               AppointmentType
   patient            Patient @relation(fields: [patientId], references: [id])
   patientId          String
```


