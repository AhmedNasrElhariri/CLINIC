# Migration `20200509225810-add-value-to-appointement-field`

This migration has been generated by etahoon at 5/9/2020, 10:58:10 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Appointment" ALTER COLUMN "glucoseLevel" SET DEFAULT -1,
ALTER COLUMN "height" SET DEFAULT -1,
ALTER COLUMN "pulse" SET DEFAULT -1,
ALTER COLUMN "temp" SET DEFAULT -1,
ALTER COLUMN "weight" SET DEFAULT -1;

ALTER TABLE "public"."AppointmentField" ADD COLUMN "value" text  NOT NULL ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200509224049-add-appointment-fields..20200509225810-add-value-to-appointement-field
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -56,37 +56,38 @@
   sex          Sex
 }
 model Appointment {
-  id               String             @default(uuid()) @id
-  date             DateTime           @default(now()) @unique
-  type             AppointmentType
-  patient          Patient            @relation(fields: [patientId], references: [id])
-  patientId        String
-  specialty        Specialty
-  doctor           User               @relation(fields: [doctorId], references: [id])
-  doctorId         String
-  labs             String?            @default("[]")
-  complain         String?            @default("")
-  signs            String?            @default("")
-  diagnosis        String?            @default("")
-  treatment        String?            @default("")
-  recommendations  String?            @default("")
-  weight           Float?             @default(-1)
-  height           Float?             @default(-1)
-  pulse            Float?             @default(-1)
-  temp             Float?             @default(-1)
-  glucoseLevel     Float?             @default(-1)
-  archived         Boolean            @default(false)
-  AppointmentField AppointmentField[]
+  id              String             @default(uuid()) @id
+  date            DateTime           @default(now()) @unique
+  type            AppointmentType
+  patient         Patient            @relation(fields: [patientId], references: [id])
+  patientId       String
+  specialty       Specialty
+  doctor          User               @relation(fields: [doctorId], references: [id])
+  doctorId        String
+  labs            String?            @default("[]")
+  complain        String?            @default("")
+  signs           String?            @default("")
+  diagnosis       String?            @default("")
+  treatment       String?            @default("")
+  recommendations String?            @default("")
+  weight          Float?             @default(-1)
+  height          Float?             @default(-1)
+  pulse           Float?             @default(-1)
+  temp            Float?             @default(-1)
+  glucoseLevel    Float?             @default(-1)
+  archived        Boolean            @default(false)
+  data            AppointmentField[]
 }
 model AppointmentField {
   id            String      @default(uuid()) @id
   appointment   Appointment @relation(fields: [appointmentId], references: [id])
   appointmentId String
   field         Field       @relation(fields: [fieldId], references: [id])
   fieldId       String
+  value         String
   @@unique([appointmentId, fieldId], name: "appointment_field_unique_constraint")
 }
```


