# Migration `20200711074156-add-labs`

This migration has been generated by etahoon at 7/11/2020, 7:41:56 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."PateintLab" (
"id" text  NOT NULL ,"name" text  NOT NULL ,"patientId" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."LabDocument" (
"fileId" text  NOT NULL ,"id" text  NOT NULL ,"pateintLabId" text  NOT NULL ,
    PRIMARY KEY ("id"))

ALTER TABLE "public"."Clinic" ALTER COLUMN "address" SET DEFAULT '',
ALTER COLUMN "doctorJobDescription" SET DEFAULT '',
ALTER COLUMN "doctorName" SET DEFAULT '',
ALTER COLUMN "doctorTitle" SET DEFAULT '',
ALTER COLUMN "phoneNo" SET DEFAULT '';

ALTER TABLE "public"."PateintLab" ADD FOREIGN KEY ("patientId")REFERENCES "public"."Patient"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."LabDocument" ADD FOREIGN KEY ("fileId")REFERENCES "public"."File"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."LabDocument" ADD FOREIGN KEY ("pateintLabId")REFERENCES "public"."PateintLab"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200709035855-set-snippet-title-unique..20200711074156-add-labs
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -69,8 +69,9 @@
   age            Int
   sex            Sex
   organizationId String
   guardianName   String?
+  pateintLab     PateintLab[]
 }
 model Appointment {
   id        String             @default(uuid()) @id
@@ -144,14 +145,15 @@
   @@unique([appointmentId, fieldId], name: "appointment_fieldId_unique_constraint")
 }
 model File {
-  id       String   @default(uuid()) @id
-  url      String
-  filename String
-  mimetype String?
-  encoding String?
-  Clinic   Clinic[]
+  id          String        @default(uuid()) @id
+  url         String
+  filename    String
+  mimetype    String?
+  encoding    String?
+  Clinic      Clinic[]
+  LabDocument LabDocument[]
 }
 model Clinic {
   id                   String  @default(uuid()) @id
@@ -196,4 +198,20 @@
   userId String
   title  String @unique
   body   String
 }
+
+model PateintLab {
+  id          String        @default(uuid()) @id
+  patient     Patient       @relation(fields: [patientId], references: [id])
+  patientId   String
+  name        String
+  LabDocument LabDocument[]
+}
+
+model LabDocument {
+  id           String     @default(uuid()) @id
+  file         File       @relation(fields: [fileId], references: [id])
+  fileId       String
+  pateintLab   PateintLab @relation(fields: [pateintLabId], references: [id])
+  pateintLabId String
+}
```


