# Migration `20200425123933-set-default-value`

This migration has been generated by etahoon at 4/25/2020, 12:39:33 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Appointment" DROP COLUMN "archived",
ADD COLUMN "archived" boolean  NOT NULL DEFAULT false,
ALTER COLUMN "complain" SET DEFAULT '',
ALTER COLUMN "diagnosis" SET DEFAULT '',
ALTER COLUMN "glucoseLevel" SET DEFAULT -1,
ALTER COLUMN "height" SET DEFAULT -1,
ALTER COLUMN "labs" SET DEFAULT '[]',
ALTER COLUMN "pulse" SET DEFAULT -1,
ALTER COLUMN "recommendations" SET DEFAULT '',
ALTER COLUMN "signs" SET DEFAULT '',
ALTER COLUMN "temp" SET DEFAULT -1,
ALTER COLUMN "treatment" SET DEFAULT '',
ALTER COLUMN "weight" SET DEFAULT -1;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200425123820..20200425123933-set-default-value
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs https//pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -52,20 +52,20 @@
   patient            Patient @relation(fields: [patientId], references: [id])
   patientId          Int
   specialty          Specialty
   doctor             User @relation(fields: [doctorId], references: [id])
-  doctorId           Int
-  labs               String? @default("")
-  complain           String? 
-  signs              String? 
-  diagnosis          String? 
-  treatment          String? 
-  recommendations    String? 
+  doctorId           Int 
+  labs               String? @default("[]")
+  complain           String? @default("")
+  signs              String? @default("")
+  diagnosis          String? @default("")
+  treatment          String? @default("")
+  recommendations    String? @default("")
-  weight             Float? 
-  height             Float? 
-  pulse              Float? 
-  temp               Float? 
-  glucoseLevel       Float? 
+  weight             Float?  @default(-1)
+  height             Float?  @default(-1)
+  pulse              Float?  @default(-1)
+  temp               Float?  @default(-1)
+  glucoseLevel       Float?  @default(-1)
   archived           Boolean @default(false)
 }
```


