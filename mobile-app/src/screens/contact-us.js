import React from 'react';
import * as R from 'ramda';

import { CRMainLayout } from '@/components';

const Patient = ({ navigation, route: { params } }) => {
  return <CRMainLayout header="Contact Us" />;
};

export default Patient;
